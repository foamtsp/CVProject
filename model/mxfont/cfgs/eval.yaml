dset:
  test:
    data_dir: data/images/test   # path to reference images
    source_font: /content/fonts_to_val/TH-Charmonman-Regular.ttf    # path to .ttf file used as the source font during the generation
    gen_chars_file: data/th_gen.json   # path to file of the characters to generate. Leave blank if you want to generate all the available characters in the source font.
